{
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@types/expect.js": "^0.3.29",
    "@types/mocha": "^2.2.32",
    "coveralls": "^2.11.9",
    "css-loader": "^0.23.1",
    "es6-promise": "^3.2.1",
    "expect.js": "^0.3.1",
    "istanbul-instrumenter-loader": "^0.2.0",
    "karma": "^0.13.22",
    "karma-chrome-launcher": "^0.2.3",
    "karma-coverage": "^0.5.5",
    "karma-firefox-launcher": "^0.1.7",
    "karma-ie-launcher": "^0.2.0",
    "karma-mocha": "^0.2.2",
    "karma-mocha-reporter": "^2.0.0",
    "karma-webpack": "^1.7.0",
    "mocha": "^2.4.5",
    "rimraf": "^2.5.2",
    "simulate-event": "^1.4.0",
    "style-loader": "^0.13.1",
    "typedoc": "^0.5.0",
    "typescript": "^2.1.5",
    "watch": "^0.18.0",
    "webpack": "^1.12.14"
  },
  "scripts": {
    "build:algorithm": "cd src/algorithm && tsc",
    "build:application": "cd src/application && tsc",
    "build:collections": "cd src/collections && tsc",
    "build:commands": "cd src/commands && tsc",
    "build:disposable": "cd src/disposable && tsc",
    "build:dragdrop": "cd src/dragdrop && tsc",
    "build:keyboard": "cd src/keyboard && tsc",
    "build:messaging": "cd src/messaging && tsc",
    "build:properties": "cd src/properties && tsc",
    "build:signaling": "cd src/signaling && tsc",
    "build:utilities": "cd src/utilities && tsc",
    "build:virtualdom": "cd src/virtualdom && tsc",
    "build:widgets": "cd src/widgets && tsc",
    "build:src": "npm run build:algorithm && npm run build:collections && npm run build:disposable && npm run build:utilities && npm run build:keyboard && npm run build:signaling && npm run build:commands && npm run build:dragdrop && npm run build:messaging && npm run build:virtualdom && npm run build:properties && npm run build:widgets && npm run build:application",
    "build:example": "cd example && tsc && webpack --config webpack.conf.js",
    "build": "npm run build:src && npm run build:example",
    "clean:algorithm": "rimraf src/algorithm/lib",
    "clean:application": "rimraf src/application/lib",
    "clean:collections": "rimraf src/collections/lib",
    "clean:commands": "rimraf src/commands/lib",
    "clean:disposable": "rimraf src/disposable/lib",
    "clean:dragdrop": "rimraf src/dragdrop/lib",
    "clean:keyboard": "rimraf src/keyboard/lib",
    "clean:messaging": "rimraf src/messaging/lib",
    "clean:properties": "rimraf src/properties/lib",
    "clean:signaling": "rimraf src/signaling/lib",
    "clean:utilities": "rimraf src/utilities/lib",
    "clean:virtualdom": "rimraf src/virtualdom/lib",
    "clean:widgets": "rimraf src/widgets/lib",
    "clean:src": "npm run clean:algorithm && npm run clean:collections && npm run clean:disposable && npm run clean:utilities && npm run clean:keyboard && npm run clean:signaling && npm run clean:commands && npm run clean:dragdrop && npm run clean:messaging && npm run clean:virtualdom && npm run clean:properties && npm run clean:widgets && npm run clean:application",
    "clean": "npm run clean:src"
  }
}
